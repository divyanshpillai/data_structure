class Solution {
public:
    long long countSubstrings(string s, char c) {
        long long res = 0;
        for(auto ch : s){
            if(ch == c){
                res++;
            }
        }

        return (res * (res+1))/2;
    }
};
